# Aegis dependencies. Python 3.11. Install into .venv:
#   pip install -r requirements.txt
# agentdojo pulls in click, python-dotenv, pydantic, rich, etc. as transitive deps.
agentdojo==0.1.35
openai==2.43.0
